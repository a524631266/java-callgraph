package gr.zhangll.javadynamic.part01.reflection;

/**
 * 1. 需要统一的接口我呢见
 * • Needs a flexible interface to support a variety of applications
 * 2. 让应用很好地配置
 * • Must be easy to configure for each application
 * 3. 不需要太好的性能，案例只是在应用生命周期之使用一次
 * • Doesn't require top performance, because the arguments are only processed once
 * 4. 不需要securityManager的参与
 * • Has no access security issues, because command line applications generally run without a
 * security manager
 */
public class ReflectDemo {

}
